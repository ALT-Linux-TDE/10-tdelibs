diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/COMMIT_EDITMSG "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/COMMIT_EDITMSG"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/COMMIT_EDITMSG	2025-02-23 15:20:42.266791409 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/COMMIT_EDITMSG"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-fix patch
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/config "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/config"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/config	2025-02-23 22:45:59.626628259 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/config"	1970-01-01 03:00:00.000000000 +0300
@@ -1,11 +0,0 @@
-[core]
-	repositoryformatversion = 0
-	filemode = true
-	bare = false
-	logallrefupdates = true
-[remote "main"]
-	url = git@github.com:ALT-Linux-TDE/10-tdelibs.git
-	fetch = +refs/heads/*:refs/remotes/main/*
-[branch "master"]
-	remote = main
-	merge = refs/heads/master
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/description "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/description"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/description	2025-02-23 13:11:02.118815673 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/description"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-Unnamed repository; edit this file 'description' to name the repository.
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/HEAD "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/HEAD"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/HEAD	2025-02-23 13:11:02.120815500 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/HEAD"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-ref: refs/heads/master
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/applypatch-msg.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/applypatch-msg.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/applypatch-msg.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/applypatch-msg.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,15 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to check the commit log message taken by
-# applypatch from an e-mail message.
-#
-# The hook should exit with non-zero status after issuing an
-# appropriate message if it wants to stop the commit.  The hook is
-# allowed to edit the commit message file.
-#
-# To enable this hook, rename this file to "applypatch-msg".
-
-. git-sh-setup
-commitmsg="$(git rev-parse --git-path hooks/commit-msg)"
-test -x "$commitmsg" && exec "$commitmsg" ${1+"$@"}
-:
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/commit-msg.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/commit-msg.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/commit-msg.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/commit-msg.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,24 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to check the commit log message.
-# Called by "git commit" with one argument, the name of the file
-# that has the commit message.  The hook should exit with non-zero
-# status after issuing an appropriate message if it wants to stop the
-# commit.  The hook is allowed to edit the commit message file.
-#
-# To enable this hook, rename this file to "commit-msg".
-
-# Uncomment the below to add a Signed-off-by line to the message.
-# Doing this in a hook is a bad idea in general, but the prepare-commit-msg
-# hook is more suited to it.
-#
-# SOB=$(git var GIT_AUTHOR_IDENT | sed -n 's/^\(.*>\).*$/Signed-off-by: \1/p')
-# grep -qs "^$SOB" "$1" || echo "$SOB" >> "$1"
-
-# This example catches duplicate Signed-off-by lines.
-
-test "" = "$(grep '^Signed-off-by: ' "$1" |
-	 sort | uniq -c | sed -e '/^[ 	]*1[ 	]/d')" || {
-	echo >&2 Duplicate Signed-off-by lines.
-	exit 1
-}
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/post-commit.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/post-commit.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/post-commit.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/post-commit.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,8 +0,0 @@
-#!/bin/sh
-#
-# An example hook script that is called after a successful
-# commit is made.
-#
-# To enable this hook, rename this file to "post-commit".
-
-: Nothing
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/post-receive.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/post-receive.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/post-receive.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/post-receive.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,15 +0,0 @@
-#!/bin/sh
-#
-# An example hook script for the "post-receive" event.
-#
-# The "post-receive" script is run after receive-pack has accepted a pack
-# and the repository has been updated.  It is passed arguments in through
-# stdin in the form
-#  <oldrev> <newrev> <refname>
-# For example:
-#  aa453216d1b3e49e7f6f98441fa56946ddcd6a20 68f7abf4e6f922807889f52bc043ecd31b79f814 refs/heads/master
-#
-# see contrib/hooks/ for a sample, or uncomment the next line and
-# rename the file to "post-receive".
-
-#. /usr/share/git-core/contrib/hooks/post-receive-email
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/post-update.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/post-update.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/post-update.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/post-update.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,8 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to prepare a packed repository for use over
-# dumb transports.
-#
-# To enable this hook, rename this file to "post-update".
-
-exec git update-server-info
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-applypatch.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-applypatch.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-applypatch.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-applypatch.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,14 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to verify what is about to be committed
-# by applypatch from an e-mail message.
-#
-# The hook should exit with non-zero status after issuing an
-# appropriate message if it wants to stop the commit.
-#
-# To enable this hook, rename this file to "pre-applypatch".
-
-. git-sh-setup
-precommit="$(git rev-parse --git-path hooks/pre-commit)"
-test -x "$precommit" && exec "$precommit" ${1+"$@"}
-:
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-commit.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-commit.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-commit.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-commit.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,54 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to verify what is about to be committed.
-# Called by "git commit" with no arguments.  The hook should
-# exit with non-zero status after issuing an appropriate message if
-# it wants to stop the commit.
-#
-# To enable this hook, rename this file to "pre-commit".
-
-if git rev-parse --verify HEAD >/dev/null 2>&1
-then
-	against=HEAD
-else
-	# Initial commit: diff against an empty tree object
-	against=$(git hash-object -t tree /dev/null)
-fi
-
-# If you want to allow non-ASCII filenames set this variable to true.
-allownonascii=$(git config --type=bool hooks.allownonascii)
-
-# Redirect output to stderr.
-exec 1>&2
-
-# Cross platform projects tend to avoid non-ASCII filenames; prevent
-# them from being added to the repository. We exploit the fact that the
-# printable range starts at the space character and ends with tilde.
-if [ "$allownonascii" != "true" ] &&
-	# Note that the use of brackets around a tr range is ok here, (it's
-	# even required, for portability to Solaris 10's /usr/bin/tr), since
-	# the square bracket bytes happen to fall in the designated range.
-	test $(git diff --cached --name-only --diff-filter=A -z $against |
-	  LC_ALL=C tr -d '[ -~]\0' | wc -c) != 0
-then
-	cat <<\EOF
-Error: Attempt to add a non-ASCII file name.
-
-This can cause problems if you want to work with people on other platforms.
-
-To be portable it is advisable to rename the file.
-
-If you know what you are doing you can disable this check using:
-
-  git config hooks.allownonascii true
-EOF
-	exit 1
-fi
-
-# If there are whitespace errors, print the offending file names and fail.
-git diff-index --check --cached $against -- || exit
-
-# When invoked by gear-commit, ensure that .gear/tags/ is up to date.
-if [ -n "${GEAR_COMMIT-}" ]; then
-	a= gear-update-tag --verify
-fi
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-merge-commit.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-merge-commit.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-merge-commit.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-merge-commit.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,13 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to verify what is about to be committed.
-# Called by "git merge" with no arguments.  The hook should
-# exit with non-zero status after issuing an appropriate message to
-# stderr if it wants to stop the merge commit.
-#
-# To enable this hook, rename this file to "pre-merge-commit".
-
-. git-sh-setup
-test -x "$GIT_DIR/hooks/pre-commit" &&
-        exec "$GIT_DIR/hooks/pre-commit"
-:
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/prepare-commit-msg.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/prepare-commit-msg.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/prepare-commit-msg.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/prepare-commit-msg.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,42 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to prepare the commit log message.
-# Called by "git commit" with the name of the file that has the
-# commit message, followed by the description of the commit
-# message's source.  The hook's purpose is to edit the commit
-# message file.  If the hook fails with a non-zero status,
-# the commit is aborted.
-#
-# To enable this hook, rename this file to "prepare-commit-msg".
-
-# This hook includes three examples. The first one removes the
-# "# Please enter the commit message..." help message.
-#
-# The second includes the output of "git diff --name-status -r"
-# into the message, just before the "git status" output.  It is
-# commented because it doesn't cope with --amend or with squashed
-# commits.
-#
-# The third example adds a Signed-off-by line to the message, that can
-# still be edited.  This is rarely a good idea.
-
-COMMIT_MSG_FILE=$1
-COMMIT_SOURCE=$2
-SHA1=$3
-
-perl -i.bak -ne 'print unless(m/^. Please enter the commit message/..m/^#$/)' "$COMMIT_MSG_FILE"
-
-# case "$COMMIT_SOURCE,$SHA1" in
-#  ,|template,)
-#    perl -i.bak -pe '
-#       print "\n" . `git diff --cached --name-status -r`
-# 	 if /^#/ && $first++ == 0' "$COMMIT_MSG_FILE" ;;
-#  *) ;;
-# esac
-
-# SOB=$(git var GIT_COMMITTER_IDENT | sed -n 's/^\(.*>\).*$/Signed-off-by: \1/p')
-# git interpret-trailers --in-place --trailer "$SOB" "$COMMIT_MSG_FILE"
-# if test -z "$COMMIT_SOURCE"
-# then
-#   perl -i.bak -pe 'print "\n" if !$first_line++' "$COMMIT_MSG_FILE"
-# fi
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-push.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-push.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-push.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-push.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,53 +0,0 @@
-#!/bin/sh
-
-# An example hook script to verify what is about to be pushed.  Called by "git
-# push" after it has checked the remote status, but before anything has been
-# pushed.  If this script exits with a non-zero status nothing will be pushed.
-#
-# This hook is called with the following parameters:
-#
-# $1 -- Name of the remote to which the push is being done
-# $2 -- URL to which the push is being done
-#
-# If pushing without using a named remote those arguments will be equal.
-#
-# Information about the commits which are being pushed is supplied as lines to
-# the standard input in the form:
-#
-#   <local ref> <local oid> <remote ref> <remote oid>
-#
-# This sample shows how to prevent push of commits where the log message starts
-# with "WIP" (work in progress).
-
-remote="$1"
-url="$2"
-
-zero=$(git hash-object --stdin </dev/null | tr '[0-9a-f]' '0')
-
-while read local_ref local_oid remote_ref remote_oid
-do
-	if test "$local_oid" = "$zero"
-	then
-		# Handle delete
-		:
-	else
-		if test "$remote_oid" = "$zero"
-		then
-			# New branch, examine all commits
-			range="$local_oid"
-		else
-			# Update to existing branch, examine new commits
-			range="$remote_oid..$local_oid"
-		fi
-
-		# Check for WIP commit
-		commit=$(git rev-list -n 1 --grep '^WIP' "$range")
-		if test -n "$commit"
-		then
-			echo >&2 "Found WIP commit in $local_ref, not pushing"
-			exit 1
-		fi
-	fi
-done
-
-exit 0
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-rebase.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-rebase.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-rebase.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-rebase.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,169 +0,0 @@
-#!/bin/sh
-#
-# Copyright (c) 2006, 2008 Junio C Hamano
-#
-# The "pre-rebase" hook is run just before "git rebase" starts doing
-# its job, and can prevent the command from running by exiting with
-# non-zero status.
-#
-# The hook is called with the following parameters:
-#
-# $1 -- the upstream the series was forked from.
-# $2 -- the branch being rebased (or empty when rebasing the current branch).
-#
-# This sample shows how to prevent topic branches that are already
-# merged to 'next' branch from getting rebased, because allowing it
-# would result in rebasing already published history.
-
-publish=next
-basebranch="$1"
-if test "$#" = 2
-then
-	topic="refs/heads/$2"
-else
-	topic=`git symbolic-ref HEAD` ||
-	exit 0 ;# we do not interrupt rebasing detached HEAD
-fi
-
-case "$topic" in
-refs/heads/??/*)
-	;;
-*)
-	exit 0 ;# we do not interrupt others.
-	;;
-esac
-
-# Now we are dealing with a topic branch being rebased
-# on top of master.  Is it OK to rebase it?
-
-# Does the topic really exist?
-git show-ref -q "$topic" || {
-	echo >&2 "No such branch $topic"
-	exit 1
-}
-
-# Is topic fully merged to master?
-not_in_master=`git rev-list --pretty=oneline ^master "$topic"`
-if test -z "$not_in_master"
-then
-	echo >&2 "$topic is fully merged to master; better remove it."
-	exit 1 ;# we could allow it, but there is no point.
-fi
-
-# Is topic ever merged to next?  If so you should not be rebasing it.
-only_next_1=`git rev-list ^master "^$topic" ${publish} | sort`
-only_next_2=`git rev-list ^master           ${publish} | sort`
-if test "$only_next_1" = "$only_next_2"
-then
-	not_in_topic=`git rev-list "^$topic" master`
-	if test -z "$not_in_topic"
-	then
-		echo >&2 "$topic is already up to date with master"
-		exit 1 ;# we could allow it, but there is no point.
-	else
-		exit 0
-	fi
-else
-	not_in_next=`git rev-list --pretty=oneline ^${publish} "$topic"`
-	perl -e '
-		my $topic = $ARGV[0];
-		my $msg = "* $topic has commits already merged to public branch:\n";
-		my (%not_in_next) = map {
-			/^([0-9a-f]+) /;
-			($1 => 1);
-		} split(/\n/, $ARGV[1]);
-		for my $elem (map {
-				/^([0-9a-f]+) (.*)$/;
-				[$1 => $2];
-			} split(/\n/, $ARGV[2])) {
-			if (!exists $not_in_next{$elem->[0]}) {
-				if ($msg) {
-					print STDERR $msg;
-					undef $msg;
-				}
-				print STDERR " $elem->[1]\n";
-			}
-		}
-	' "$topic" "$not_in_next" "$not_in_master"
-	exit 1
-fi
-
-<<\DOC_END
-
-This sample hook safeguards topic branches that have been
-published from being rewound.
-
-The workflow assumed here is:
-
- * Once a topic branch forks from "master", "master" is never
-   merged into it again (either directly or indirectly).
-
- * Once a topic branch is fully cooked and merged into "master",
-   it is deleted.  If you need to build on top of it to correct
-   earlier mistakes, a new topic branch is created by forking at
-   the tip of the "master".  This is not strictly necessary, but
-   it makes it easier to keep your history simple.
-
- * Whenever you need to test or publish your changes to topic
-   branches, merge them into "next" branch.
-
-The script, being an example, hardcodes the publish branch name
-to be "next", but it is trivial to make it configurable via
-$GIT_DIR/config mechanism.
-
-With this workflow, you would want to know:
-
-(1) ... if a topic branch has ever been merged to "next".  Young
-    topic branches can have stupid mistakes you would rather
-    clean up before publishing, and things that have not been
-    merged into other branches can be easily rebased without
-    affecting other people.  But once it is published, you would
-    not want to rewind it.
-
-(2) ... if a topic branch has been fully merged to "master".
-    Then you can delete it.  More importantly, you should not
-    build on top of it -- other people may already want to
-    change things related to the topic as patches against your
-    "master", so if you need further changes, it is better to
-    fork the topic (perhaps with the same name) afresh from the
-    tip of "master".
-
-Let's look at this example:
-
-		   o---o---o---o---o---o---o---o---o---o "next"
-		  /       /           /           /
-		 /   a---a---b A     /           /
-		/   /               /           /
-	       /   /   c---c---c---c B         /
-	      /   /   /             \         /
-	     /   /   /   b---b C     \       /
-	    /   /   /   /             \     /
-    ---o---o---o---o---o---o---o---o---o---o---o "master"
-
-
-A, B and C are topic branches.
-
- * A has one fix since it was merged up to "next".
-
- * B has finished.  It has been fully merged up to "master" and "next",
-   and is ready to be deleted.
-
- * C has not merged to "next" at all.
-
-We would want to allow C to be rebased, refuse A, and encourage
-B to be deleted.
-
-To compute (1):
-
-	git rev-list ^master ^topic next
-	git rev-list ^master        next
-
-	if these match, topic has not merged in next at all.
-
-To compute (2):
-
-	git rev-list master..topic
-
-	if this is empty, it is fully merged to "master".
-
-DOC_END
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-receive.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-receive.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/pre-receive.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/pre-receive.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,24 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to make use of push options.
-# The example simply echoes all push options that start with 'echoback='
-# and rejects all pushes when the "reject" push option is used.
-#
-# To enable this hook, rename this file to "pre-receive".
-
-if test -n "$GIT_PUSH_OPTION_COUNT"
-then
-	i=0
-	while test "$i" -lt "$GIT_PUSH_OPTION_COUNT"
-	do
-		eval "value=\$GIT_PUSH_OPTION_$i"
-		case "$value" in
-		echoback=*)
-			echo "echo from the pre-receive-hook: ${value#*=}" >&2
-			;;
-		reject)
-			exit 1
-		esac
-		i=$((i + 1))
-	done
-fi
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/push-to-checkout.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/push-to-checkout.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/push-to-checkout.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/push-to-checkout.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,78 +0,0 @@
-#!/bin/sh
-
-# An example hook script to update a checked-out tree on a git push.
-#
-# This hook is invoked by git-receive-pack(1) when it reacts to git
-# push and updates reference(s) in its repository, and when the push
-# tries to update the branch that is currently checked out and the
-# receive.denyCurrentBranch configuration variable is set to
-# updateInstead.
-#
-# By default, such a push is refused if the working tree and the index
-# of the remote repository has any difference from the currently
-# checked out commit; when both the working tree and the index match
-# the current commit, they are updated to match the newly pushed tip
-# of the branch. This hook is to be used to override the default
-# behaviour; however the code below reimplements the default behaviour
-# as a starting point for convenient modification.
-#
-# The hook receives the commit with which the tip of the current
-# branch is going to be updated:
-commit=$1
-
-# It can exit with a non-zero status to refuse the push (when it does
-# so, it must not modify the index or the working tree).
-die () {
-	echo >&2 "$*"
-	exit 1
-}
-
-# Or it can make any necessary changes to the working tree and to the
-# index to bring them to the desired state when the tip of the current
-# branch is updated to the new commit, and exit with a zero status.
-#
-# For example, the hook can simply run git read-tree -u -m HEAD "$1"
-# in order to emulate git fetch that is run in the reverse direction
-# with git push, as the two-tree form of git read-tree -u -m is
-# essentially the same as git switch or git checkout that switches
-# branches while keeping the local changes in the working tree that do
-# not interfere with the difference between the branches.
-
-# The below is a more-or-less exact translation to shell of the C code
-# for the default behaviour for git's push-to-checkout hook defined in
-# the push_to_deploy() function in builtin/receive-pack.c.
-#
-# Note that the hook will be executed from the repository directory,
-# not from the working tree, so if you want to perform operations on
-# the working tree, you will have to adapt your code accordingly, e.g.
-# by adding "cd .." or using relative paths.
-
-if ! git update-index -q --ignore-submodules --refresh
-then
-	die "Up-to-date check failed"
-fi
-
-if ! git diff-files --quiet --ignore-submodules --
-then
-	die "Working directory has unstaged changes"
-fi
-
-# This is a rough translation of:
-#
-#   head_has_history() ? "HEAD" : EMPTY_TREE_SHA1_HEX
-if git cat-file -e HEAD 2>/dev/null
-then
-	head=HEAD
-else
-	head=$(git hash-object -t tree --stdin </dev/null)
-fi
-
-if ! git diff-index --quiet --cached --ignore-submodules $head --
-then
-	die "Working directory has staged changes"
-fi
-
-if ! git read-tree -u -m "$commit"
-then
-	die "Could not update working tree to new HEAD"
-fi
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/sendemail-validate.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/sendemail-validate.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/sendemail-validate.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/sendemail-validate.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,77 +0,0 @@
-#!/bin/sh
-
-# An example hook script to validate a patch (and/or patch series) before
-# sending it via email.
-#
-# The hook should exit with non-zero status after issuing an appropriate
-# message if it wants to prevent the email(s) from being sent.
-#
-# To enable this hook, rename this file to "sendemail-validate".
-#
-# By default, it will only check that the patch(es) can be applied on top of
-# the default upstream branch without conflicts in a secondary worktree. After
-# validation (successful or not) of the last patch of a series, the worktree
-# will be deleted.
-#
-# The following config variables can be set to change the default remote and
-# remote ref that are used to apply the patches against:
-#
-#   sendemail.validateRemote (default: origin)
-#   sendemail.validateRemoteRef (default: HEAD)
-#
-# Replace the TODO placeholders with appropriate checks according to your
-# needs.
-
-validate_cover_letter () {
-	file="$1"
-	# TODO: Replace with appropriate checks (e.g. spell checking).
-	true
-}
-
-validate_patch () {
-	file="$1"
-	# Ensure that the patch applies without conflicts.
-	git am -3 "$file" || return
-	# TODO: Replace with appropriate checks for this patch
-	# (e.g. checkpatch.pl).
-	true
-}
-
-validate_series () {
-	# TODO: Replace with appropriate checks for the whole series
-	# (e.g. quick build, coding style checks, etc.).
-	true
-}
-
-# main -------------------------------------------------------------------------
-
-if test "$GIT_SENDEMAIL_FILE_COUNTER" = 1
-then
-	remote=$(git config --default origin --get sendemail.validateRemote) &&
-	ref=$(git config --default HEAD --get sendemail.validateRemoteRef) &&
-	worktree=$(mktemp --tmpdir -d sendemail-validate.XXXXXXX) &&
-	git worktree add -fd --checkout "$worktree" "refs/remotes/$remote/$ref" &&
-	git config --replace-all sendemail.validateWorktree "$worktree"
-else
-	worktree=$(git config --get sendemail.validateWorktree)
-fi || {
-	echo "sendemail-validate: error: failed to prepare worktree" >&2
-	exit 1
-}
-
-unset GIT_DIR GIT_WORK_TREE
-cd "$worktree" &&
-
-if grep -q "^diff --git " "$1"
-then
-	validate_patch "$1"
-else
-	validate_cover_letter "$1"
-fi &&
-
-if test "$GIT_SENDEMAIL_FILE_COUNTER" = "$GIT_SENDEMAIL_FILE_TOTAL"
-then
-	git config --unset-all sendemail.validateWorktree &&
-	trap 'git worktree remove -ff "$worktree"' EXIT &&
-	validate_series
-fi
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/update.sample "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/update.sample"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/hooks/update.sample	2024-11-27 00:14:58.000000000 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/hooks/update.sample"	1970-01-01 03:00:00.000000000 +0300
@@ -1,144 +0,0 @@
-#!/bin/sh
-#
-# An example hook script to block unannotated tags from entering.
-# Called by "git receive-pack" with arguments: refname sha1-old sha1-new
-#
-# To enable this hook, rename this file to "update".
-#
-# Config
-# ------
-# hooks.allowbadwhitespace
-#   This boolean sets whether a commit that adds bad whitespace will be
-#   allowed in the repository.  By default they won't be.
-# hooks.allowunannotated
-#   This boolean sets whether unannotated tags will be allowed into the
-#   repository.  By default they won't be.
-# hooks.allowdeletetag
-#   This boolean sets whether deleting tags will be allowed in the
-#   repository.  By default they won't be.
-# hooks.allowmodifytag
-#   This boolean sets whether a tag may be modified after creation. By default
-#   it won't be.
-# hooks.allowdeletebranch
-#   This boolean sets whether deleting branches will be allowed in the
-#   repository.  By default they won't be.
-# hooks.denycreatebranch
-#   This boolean sets whether remotely creating branches will be denied
-#   in the repository.  By default this is allowed.
-#
-
-# --- Command line
-refname="$1"
-oldrev="$2"
-newrev="$3"
-
-# --- Safety check
-if [ -z "$GIT_DIR" ]; then
-	echo "Don't run this script from the command line." >&2
-	echo " (if you want, you could supply GIT_DIR then run" >&2
-	echo "  $0 <ref> <oldrev> <newrev>)" >&2
-	exit 1
-fi
-
-if [ -z "$refname" -o -z "$oldrev" -o -z "$newrev" ]; then
-	echo "usage: $0 <ref> <oldrev> <newrev>" >&2
-	exit 1
-fi
-
-# --- Config
-allowunannotated=$(git config --type=bool hooks.allowunannotated)
-allowdeletebranch=$(git config --type=bool hooks.allowdeletebranch)
-denycreatebranch=$(git config --type=bool hooks.denycreatebranch)
-allowdeletetag=$(git config --type=bool hooks.allowdeletetag)
-allowmodifytag=$(git config --type=bool hooks.allowmodifytag)
-
-# check for no description
-projectdesc=$(sed -e '1q' "$GIT_DIR/description")
-case "$projectdesc" in
-"Unnamed repository"* | "")
-	echo "*** Project description file hasn't been set" >&2
-	exit 1
-	;;
-esac
-
-# --- Check types
-# if $newrev is 0000...0000, it's a commit to delete a ref.
-zero=$(git hash-object --stdin </dev/null | tr '[0-9a-f]' '0')
-if [ "$newrev" = "$zero" ]; then
-	newrev_type=delete
-else
-	newrev_type=$(git cat-file -t $newrev)
-fi
-
-check_diff=no
-case "$refname","$newrev_type" in
-	refs/tags/*,commit)
-		# un-annotated tag
-		short_refname=${refname##refs/tags/}
-		if [ "$allowunannotated" != "true" ]; then
-			echo "*** The un-annotated tag, $short_refname, is not allowed in this repository" >&2
-			echo "*** Use 'git tag [ -a | -s ]' for tags you want to propagate." >&2
-			exit 1
-		fi
-		;;
-	refs/tags/*,delete)
-		# delete tag
-		if [ "$allowdeletetag" != "true" ]; then
-			echo "*** Deleting a tag is not allowed in this repository" >&2
-			exit 1
-		fi
-		;;
-	refs/tags/*,tag)
-		# annotated tag
-		if [ "$allowmodifytag" != "true" ] && git rev-parse $refname > /dev/null 2>&1
-		then
-			echo "*** Tag '$refname' already exists." >&2
-			echo "*** Modifying a tag is not allowed in this repository." >&2
-			exit 1
-		fi
-		;;
-	refs/heads/*,commit)
-		# branch
-		check_diff=yes
-		if [ "$oldrev" = "$zero" -a "$denycreatebranch" = "true" ]; then
-			echo "*** Creating a branch is not allowed in this repository" >&2
-			exit 1
-		fi
-		;;
-	refs/heads/*,delete)
-		# delete branch
-		if [ "$allowdeletebranch" != "true" ]; then
-			echo "*** Deleting a branch is not allowed in this repository" >&2
-			exit 1
-		fi
-		;;
-	refs/remotes/*,commit)
-		# tracking branch
-		check_diff=yes
-		;;
-	refs/remotes/*,delete)
-		# delete tracking branch
-		if [ "$allowdeletebranch" != "true" ]; then
-			echo "*** Deleting a tracking branch is not allowed in this repository" >&2
-			exit 1
-		fi
-		;;
-	*)
-		# Anything else (is there anything else?)
-		echo "*** Update hook: unknown type of update to ref $refname of type $newrev_type" >&2
-		exit 1
-		;;
-esac
-
-if [ $check_diff = yes ]; then
-	allow_bad_whitespace=$(git config --bool hooks.allowbadwhitespace)
-	if [ "$allow_bad_whitespace" != true ]; then
-		# If pushing a new branch, compare it with the empty object.
-		[ "$oldrev" != "$zero" ] ||
-			oldrev=4b825dc642cb6eb9a060e54bf8d69288fbee4904
-		exec git diff --check "$oldrev" "$newrev" --
-	fi
-fi
-
-# --- Finished
-exit 0
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/index и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/index' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/info/exclude "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/info/exclude"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/info/exclude	2025-02-23 13:11:02.118815673 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/info/exclude"	1970-01-01 03:00:00.000000000 +0300
@@ -1,6 +0,0 @@
-# git ls-files --others --exclude-from=.git/info/exclude
-# Lines that start with '#' are comments.
-# For a project mostly in C, the following would be a good set of
-# exclude patterns (uncomment them if you want to use them):
-# *.[oa]
-# *~
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/info/refs "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/info/refs"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/info/refs	2025-02-23 13:11:42.212354297 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/info/refs"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-c2ed875d0773e75f4b19322f00c402ebcefbd285	refs/heads/master
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/logs/HEAD "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/logs/HEAD"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/logs/HEAD	2025-02-23 15:20:42.267791322 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/logs/HEAD"	1970-01-01 03:00:00.000000000 +0300
@@ -1,6 +0,0 @@
-0000000000000000000000000000000000000000 c2ed875d0773e75f4b19322f00c402ebcefbd285 Petr Akhlamov <ahlamovpm@basealt.ru> 1740305496 +0300	commit (initial): Добавлены наработки
-c2ed875d0773e75f4b19322f00c402ebcefbd285 3983fa170bbaa8fbe5ff0547055ff524a468dee6 Petr Akhlamov <ahlamovpm@basealt.ru> 1740305947 +0300	commit: fix патч
-3983fa170bbaa8fbe5ff0547055ff524a468dee6 1217108e1ed84e3788ab901ea61ef5e517a6e445 Petr Akhlamov <ahlamovpm@basealt.ru> 1740307576 +0300	commit: fix spec - убираем строчку об include/libltdl
-1217108e1ed84e3788ab901ea61ef5e517a6e445 ef93c5369e276af71bff5190363bc3d143e1f798 Petr Akhlamov <ahlamovpm@basealt.ru> 1740307745 +0300	commit: fix spec - убираем строчку об include/libltdl
-ef93c5369e276af71bff5190363bc3d143e1f798 7725a422760a817b05a8afa6e129e5a42d401e5d Petr Akhlamov <ahlamovpm@basealt.ru> 1740311247 +0300	commit: fix spec
-7725a422760a817b05a8afa6e129e5a42d401e5d 689d07201b37ce6357efc395992aef70ec712f16 Petr Akhlamov <ahlamovpm@basealt.ru> 1740313242 +0300	commit: fix patch
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/logs/refs/heads/master "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/logs/refs/heads/master"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/logs/refs/heads/master	2025-02-23 15:20:42.267791322 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/logs/refs/heads/master"	1970-01-01 03:00:00.000000000 +0300
@@ -1,6 +0,0 @@
-0000000000000000000000000000000000000000 c2ed875d0773e75f4b19322f00c402ebcefbd285 Petr Akhlamov <ahlamovpm@basealt.ru> 1740305496 +0300	commit (initial): Добавлены наработки
-c2ed875d0773e75f4b19322f00c402ebcefbd285 3983fa170bbaa8fbe5ff0547055ff524a468dee6 Petr Akhlamov <ahlamovpm@basealt.ru> 1740305947 +0300	commit: fix патч
-3983fa170bbaa8fbe5ff0547055ff524a468dee6 1217108e1ed84e3788ab901ea61ef5e517a6e445 Petr Akhlamov <ahlamovpm@basealt.ru> 1740307576 +0300	commit: fix spec - убираем строчку об include/libltdl
-1217108e1ed84e3788ab901ea61ef5e517a6e445 ef93c5369e276af71bff5190363bc3d143e1f798 Petr Akhlamov <ahlamovpm@basealt.ru> 1740307745 +0300	commit: fix spec - убираем строчку об include/libltdl
-ef93c5369e276af71bff5190363bc3d143e1f798 7725a422760a817b05a8afa6e129e5a42d401e5d Petr Akhlamov <ahlamovpm@basealt.ru> 1740311247 +0300	commit: fix spec
-7725a422760a817b05a8afa6e129e5a42d401e5d 689d07201b37ce6357efc395992aef70ec712f16 Petr Akhlamov <ahlamovpm@basealt.ru> 1740313242 +0300	commit: fix patch
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/logs/refs/remotes/main/master "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/logs/refs/remotes/main/master"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/logs/refs/remotes/main/master	2025-02-23 22:45:59.636627986 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/logs/refs/remotes/main/master"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-0000000000000000000000000000000000000000 689d07201b37ce6357efc395992aef70ec712f16 Petr Akhlamov <ahlamovpm@basealt.ru> 1740339959 +0300	update by push
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/0a/117e35a37c63ddd95e2fe4d658e0e474175d29 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/0a/117e35a37c63ddd95e2fe4d658e0e474175d29' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/12/17108e1ed84e3788ab901ea61ef5e517a6e445 "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/12/17108e1ed84e3788ab901ea61ef5e517a6e445"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/12/17108e1ed84e3788ab901ea61ef5e517a6e445	2025-02-23 13:46:16.814793532 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/12/17108e1ed84e3788ab901ea61ef5e517a6e445"	1970-01-01 03:00:00.000000000 +0300
@@ -1,4 +0,0 @@
-x��AN�0�a�>��0��8�*7�
-c{�F8M�:�K��CH����܀��/���8}��k.rb��d��д:���Ao�u���PL�x�A�;he�Z�.X�!�V��(Rm�[Asގ	8'��F�'��?L��Ҟ)��4���(�h�i�r
-��2��"��O��
-걜�g}.��|C}��K��[��G(�r�~����&�6f�"h;
\ В конце файла нет новой строки
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/2c/2812cff9aed0b2eb891fbb08f6b63d8bb40c00 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/2c/2812cff9aed0b2eb891fbb08f6b63d8bb40c00' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/39/83fa170bbaa8fbe5ff0547055ff524a468dee6 "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/39/83fa170bbaa8fbe5ff0547055ff524a468dee6"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/39/83fa170bbaa8fbe5ff0547055ff524a468dee6	2025-02-23 13:19:07.238098966 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/39/83fa170bbaa8fbe5ff0547055ff524a468dee6"	1970-01-01 03:00:00.000000000 +0300
@@ -1,2 +0,0 @@
-x���	�@E]Oo/ț_&"ځ-��	Ɖ�vc;v`IG��ݹ.��w][@H�*�B���P:����"O$��T��.ӥ@k�##��<�R��
-򁒏��̍��3�d8�����l��n�ݕܹl�nJ�VX/�,|+��g�����^�c~��}M�
\ В конце файла нет новой строки
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/63/58f6e0bf30bb82554f27ed968726d80a86a962 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/63/58f6e0bf30bb82554f27ed968726d80a86a962' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/68/9d07201b37ce6357efc395992aef70ec712f16 "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/68/9d07201b37ce6357efc395992aef70ec712f16"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/68/9d07201b37ce6357efc395992aef70ec712f16	2025-02-23 15:20:42.267791322 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/68/9d07201b37ce6357efc395992aef70ec712f16"	1970-01-01 03:00:00.000000000 +0300
@@ -1,3 +0,0 @@
-x��[
-�0E��*�_���UADw�&Ʉ[[b*.����n��ih@7��@�2��c�=z�vyo��8o4������@�-Q�ȝw\؋��|L���e�k��
-i��~�i~���L��w���z,m�l� ��}���^��	�ԫ{7IU
\ В конце файла нет новой строки
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/70/69451f97f986576f2eedca68b91123d27f5e16 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/70/69451f97f986576f2eedca68b91123d27f5e16' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/77/25a422760a817b05a8afa6e129e5a42d401e5d и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/77/25a422760a817b05a8afa6e129e5a42d401e5d' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/af/47baeca02bd600ac7abaf388470dd072f50ce6 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/af/47baeca02bd600ac7abaf388470dd072f50ce6' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/bd/ac70e034a076af70ccfdb3320d7cac965eaea0 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/bd/ac70e034a076af70ccfdb3320d7cac965eaea0' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/cd/dbe1245f79d9925ac7045febee38d996be4016 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/cd/dbe1245f79d9925ac7045febee38d996be4016' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/d1/da3a8a2127e3f0405df852831e864c45f6ec18 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/d1/da3a8a2127e3f0405df852831e864c45f6ec18' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/d4/ab3b28450cecf0ad226ad471da7d0a83781c26 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/d4/ab3b28450cecf0ad226ad471da7d0a83781c26' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/e1/053a54a130166645dc2e50ae5ec6e156f645f5 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/e1/053a54a130166645dc2e50ae5ec6e156f645f5' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/ef/93c5369e276af71bff5190363bc3d143e1f798 "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/ef/93c5369e276af71bff5190363bc3d143e1f798"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/ef/93c5369e276af71bff5190363bc3d143e1f798	2025-02-23 13:49:05.171207525 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/ef/93c5369e276af71bff5190363bc3d143e1f798"	1970-01-01 03:00:00.000000000 +0300
@@ -1,3 +0,0 @@
-x��AJ�0@]�/j��M
-"z�����iɤ�R���q����9�ܽ�x��<Mc��YN̠�vJ�z�A�fr�
-D҅��fpDFz)ł���VVIǊg���!�R1v�C˭�ر1��5o����=l"N�#\�,�-�1�˴ހ�F6�Z���	�𿗙�ۋ0>�naP��P>�sy/��K}=ɱ�����r,�>�_őb���g
\ В конце файла нет новой строки
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/f5/7fb714c66c3e8fc5ec54b48e6c2fa2e4cf39b8 и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/f5/7fb714c66c3e8fc5ec54b48e6c2fa2e4cf39b8' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/f8/d2c86f1bbb6ca6a2063a6aa5d94a2fe563102e и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/f8/d2c86f1bbb6ca6a2063a6aa5d94a2fe563102e' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/info/commit-graph и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/info/commit-graph' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/info/packs "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/info/packs"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/info/packs	2025-02-23 13:11:42.212354297 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/info/packs"	1970-01-01 03:00:00.000000000 +0300
@@ -1,2 +0,0 @@
-P pack-f6b15be46f38b5ad498c524755c7ff159bfef238.pack
-
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/pack/pack-f6b15be46f38b5ad498c524755c7ff159bfef238.idx и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/pack/pack-f6b15be46f38b5ad498c524755c7ff159bfef238.idx' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/pack/pack-f6b15be46f38b5ad498c524755c7ff159bfef238.pack и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/pack/pack-f6b15be46f38b5ad498c524755c7ff159bfef238.pack' различаются
Двоичные файлы /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/objects/pack/pack-f6b15be46f38b5ad498c524755c7ff159bfef238.rev и '/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/objects/pack/pack-f6b15be46f38b5ad498c524755c7ff159bfef238.rev' различаются
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/packed-refs "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/packed-refs"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/packed-refs	2025-02-23 13:11:36.906811746 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/packed-refs"	1970-01-01 03:00:00.000000000 +0300
@@ -1,2 +0,0 @@
-# pack-refs with: peeled fully-peeled sorted 
-c2ed875d0773e75f4b19322f00c402ebcefbd285 refs/heads/master
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/refs/heads/master "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/refs/heads/master"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/refs/heads/master	2025-02-23 15:20:42.267791322 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/refs/heads/master"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-689d07201b37ce6357efc395992aef70ec712f16
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/refs/remotes/main/master "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/refs/remotes/main/master"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/.git/refs/remotes/main/master	2025-02-23 22:45:59.631628123 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/.git/refs/remotes/main/master"	1970-01-01 03:00:00.000000000 +0300
@@ -1 +0,0 @@
-689d07201b37ce6357efc395992aef70ec712f16
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/tde-tdelibs/CMakeLists.txt "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/tde-tdelibs/CMakeLists.txt"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/tde-tdelibs/CMakeLists.txt	2025-01-27 17:49:12.695945145 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/tde-tdelibs/CMakeLists.txt"	2025-02-23 12:07:50.567984921 +0300
@@ -21,6 +21,8 @@
 
 project( tdelibs )
 
+include_directories(${LIBLTDL_INCLUDE_DIR})
+
 set( PACKAGE tdelibs )
 
 
@@ -1054,16 +1056,16 @@
 
 ##### check for aRts ############################
 
-if( WITH_ARTS )
-  pkg_search_module( ARTS arts )
-  if( ARTS_FOUND )
-    set( ARTS_MCOPIDL_EXECUTABLE ${ARTS_PREFIX}/bin/mcopidl )
-  else( ARTS_FOUND )
-    message(FATAL_ERROR "\naRts are requested, but not found on your system" )
-  endif( ARTS_FOUND )
-else( WITH_ARTS )
-  set( WITHOUT_ARTS 1 )
-endif( WITH_ARTS )
+#if( WITH_ARTS )
+#  pkg_search_module( ARTS arts )
+#  if( ARTS_FOUND )
+#    set( ARTS_MCOPIDL_EXECUTABLE ${ARTS_PREFIX}/bin/mcopidl )
+#  else( ARTS_FOUND )
+#    message(FATAL_ERROR "\naRts are requested, but not found on your system" )
+#  endif( ARTS_FOUND )
+#else( WITH_ARTS )
+#  set( WITHOUT_ARTS 1 )
+#endif( WITH_ARTS )
 
 
 ##### check for Xft #############################
@@ -1460,7 +1462,7 @@
   add_subdirectory( tdelfeditor )
 endif( WITH_ELFICON )
 add_subdirectory( dcop )
-add_subdirectory( libltdl )
+#add_subdirectory( libltdl )
 add_subdirectory( kglib )
 add_subdirectory( tdefx )
 add_subdirectory( tdecore )
diff -urN /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/tde-tdelibs.spec "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/tde-tdelibs.spec"
--- /home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs/tde-tdelibs.spec	2025-02-23 14:46:42.133387187 +0300
+++ "/home/user/_MYPROJECTS/TDE-github/10-tdelibs/10-tdelibs (patch)/tde-tdelibs.spec"	2025-02-23 14:41:38.981871500 +0300
@@ -485,9 +485,9 @@
   -DWITH_TDEICONLOADER_DEBUG=OFF \
   -DUTEMPTER_HELPER=/usr/lib/utempter/utempter \
   -DLIBLTDL_LIBRARY="/usr/lib64/libltdl.so" \
- ..
-#add support of libltdl
-#  -DLIBLTDL_INCLUDE_DIR="/usr/include/libltdl" \ 
+#add libltdl support
+#  -DLIBLTDL_INCLUDE_DIR="/usr/include/libltdl" \
+  ..
 
 %make_build || make
 
